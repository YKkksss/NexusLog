# Django settings
DJANGO_SECRET_KEY="django-insecure-#e)hz#vp1k98*acoy42x%1pu=t!9(driqn-=5etm#av8rn&s8%"
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS="*"

# Database settings (choose one block and fill in your details)

# Option 1: SQLite (default if no other DB settings are provided in .env)
# DB_ENGINE=django.db.backends.sqlite3
# DB_NAME=db.sqlite3

# Option 2: PostgreSQL
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=your_postgres_db_name
# DB_USER=your_postgres_user
# DB_PASSWORD=your_postgres_password
# DB_HOST=localhost
# DB_PORT=5432

# Option 3: MySQL
DB_ENGINE=django.db.backends.mysql
DB_NAME=blog
DB_USER=root
DB_PASSWORD=04150522
DB_HOST=localhost
DB_PORT=23306

# Email settings (if applicable)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your_email@example.com
# EMAIL_HOST_PASSWORD=your_email_password
# DEFAULT_FROM_EMAIL=webmaster@example.com

# Cloud Storage (e.g., AWS S3 - if applicable)
# AWS_ACCESS_KEY_ID=your_aws_access_key_id
# AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
# AWS_STORAGE_BUCKET_NAME=your_s3_bucket_name
# AWS_S3_REGION_NAME=your_s3_region
# AWS_S3_CUSTOM_DOMAIN=your_s3_custom_domain (optional)
# STATICFILES_STORAGE=storages.backends.s3boto3.S3StaticStorage (example)
# DEFAULT_FILE_STORAGE=storages.backends.s3boto3.S3Boto3Storage (example)

# Other application-specific settings
# API_KEY_SERVICE_X=your_api_key_for_service_x
# THIRD_PARTY_URL=https://api.example.com/